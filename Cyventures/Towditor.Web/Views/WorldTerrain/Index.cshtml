@model IEnumerable<Towditor.Web.EFModel.WorldTerrains>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Terrain)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.World)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Terrain.TerrainName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.World.WorldName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.WorldTerrainId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.WorldTerrainId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.WorldTerrainId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
